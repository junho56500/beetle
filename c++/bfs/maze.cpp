#include "stdio.h"
#include <vector>
#include <stack>

using namespace std;

findWay(const vector<char>& maze)
{
    vector<vector<int>> visited;
    stack<vector<int>> s = {{0,0}};

    while (stack.size())
    {
        visited
    }

    visited = []
    while 


    
}

int main(){

    std::vector<std::vector<char>> maze1 = {
        {'S', '.', '.'},
        {'#', '#', '.'},
        {'.', '.', 'E'}
    };

    findWay(maze1);
    
    
    return 0;
}